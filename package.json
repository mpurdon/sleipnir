{
  "name": "sleipnir",
  "private": true,
  "version": "0.1.10",
  "type": "module",
  "scripts": {
    "dev": "bun --hot dev-server.ts",
    "build": "bun build ./index.html --outdir=dist --minify --sourcemap=linked",
    "tauri": "tauri",
    "docs:build": "bun run scripts/build-docs.ts",
    "docs:serve": "bun run scripts/build-docs.ts && bun run scripts/serve-docs.ts",
    "test": "bun test"
  },
  "dependencies": {
    "react": "^19",
    "react-dom": "^19",
    "@fontsource/jetbrains-mono": "^5"
  },
  "devDependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/cli": "^2",
    "@tauri-apps/plugin-opener": "^2",
    "@types/bun": "^1",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "marked": "^18.0.11",
    "typescript": "^5"
  }
}
